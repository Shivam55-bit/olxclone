```
╔════════════════════════════════════════════════════════════════════════════╗
║                    🌍 GEOLOCATION QUICK START CARD 🌍                       ║
║              React Native - Get Current Location on Button Click           ║
╚════════════════════════════════════════════════════════════════════════════╝

📍 IN 4 STEPS (Total: 20 minutes)
══════════════════════════════════════════════════════════════════════════════

STEP 1: Install Package (5 minutes)
───────────────────────────────────────────────────────────────────────────────
$ npm install react-native-geolocation-service
$ npx react-native link react-native-geolocation-service
$ cd ios && pod install && cd ..

✅ Check: No errors should appear


STEP 2: Android Setup (5 minutes)
───────────────────────────────────────────────────────────────────────────────
File: android/app/src/main/AndroidManifest.xml

Add BEFORE <application> tag:

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

✅ Check: File should be valid XML (no red squiggly lines)


STEP 3: iOS Setup (5 minutes)
───────────────────────────────────────────────────────────────────────────────
File: ios/olxclone/Info.plist

Add INSIDE <dict> tag (before closing </dict>):

    <key>NSLocationWhenInUseUsageDescription</key>
    <string>This app needs your location to show nearby items.</string>
    
    <key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
    <string>This app needs your location to show nearby items.</string>
    
    <key>NSLocationAlwaysUsageDescription</key>
    <string>This app needs your location to show nearby items.</string>

✅ Check: File should be valid XML (no red squiggly lines)


STEP 4: Use in Component (5 minutes)
───────────────────────────────────────────────────────────────────────────────
Copy locationHelper.js to: src/utils/locationHelper.js
(Already created for you! ✅)

Then in any component:

    import { getCurrentLocation } from '../utils/locationHelper';
    
    const handleGetLocation = async () => {
      const location = await getCurrentLocation();
      if (location) {
        console.log('Latitude:', location.latitude);
        console.log('Longitude:', location.longitude);
      }
    };
    
    <TouchableOpacity onPress={handleGetLocation}>
      <Ionicons name="locate" size={20} />
      <Text>Get My Location</Text>
    </TouchableOpacity>

✅ Check: Button works and returns coordinates

═══════════════════════════════════════════════════════════════════════════════

🧪 QUICK TEST
───────────────────────────────────────────────────────────────────────────────
1. Run: npm run android (or npm run ios)
2. When app opens, TAP the location button
3. ALLOW permission when prompted
4. Should display latitude and longitude ✅

═══════════════════════════════════════════════════════════════════════════════

📊 WHAT YOU GET
───────────────────────────────────────────────────────────────────────────────
✅ Automatic permission handling (Android)
✅ User-friendly error messages
✅ GPS coordinates (latitude, longitude)
✅ Accuracy measurement
✅ Works on Android & iOS
✅ Handles all error cases

═══════════════════════════════════════════════════════════════════════════════

🔍 RESPONSE FORMAT
───────────────────────────────────────────────────────────────────────────────
{
  latitude: 28.6448,
  longitude: 77.2167,
  accuracy: 5.2,
  address: "28.6448, 77.2167"
}

═══════════════════════════════════════════════════════════════════════════════

⚡ MOST COMMON PATTERNS
───────────────────────────────────────────────────────────────────────────────

Pattern 1: Simple Button
────────────────────────
<TouchableOpacity onPress={handleGetLocation}>
  <Text>Get Location</Text>
</TouchableOpacity>


Pattern 2: With Loading State
─────────────────────────────
<TouchableOpacity onPress={handleGetLocation} disabled={loading}>
  <ActivityIndicator animating={loading} />
  <Text>{loading ? 'Fetching...' : 'Get Location'}</Text>
</TouchableOpacity>


Pattern 3: Display Coordinates
──────────────────────────────
{location && (
  <View>
    <Text>📍 {location.latitude.toFixed(6)}</Text>
    <Text>📍 {location.longitude.toFixed(6)}</Text>
  </View>
)}


Pattern 4: Send to API
─────────────────────
const location = await getCurrentLocation();
api.get('/api/nearby', {
  latitude: location.latitude,
  longitude: location.longitude,
});

═══════════════════════════════════════════════════════════════════════════════

⚠️ COMMON ISSUES
───────────────────────────────────────────────────────────────────────────────

Issue: "Permission denied"
→ Check AndroidManifest.xml has both permissions
→ Settings > Apps > [Your App] > Permissions > Allow Location

Issue: "GPS not available"
→ Enable GPS on emulator/device
→ Emulator: Extended Controls > Location > Set coordinates

Issue: "Timeout error"
→ Ensure GPS is enabled
→ Try from location with strong GPS signal
→ Try again

Issue: "Can't find locationHelper.js"
→ Make sure file is at: src/utils/locationHelper.js
→ Check import path in your component

═══════════════════════════════════════════════════════════════════════════════

📚 DOCUMENTATION
───────────────────────────────────────────────────────────────────────────────
For more details, see:

• README_GEOLOCATION.md - Main guide & index
• GEOLOCATION_GUIDE.md - Complete reference
• QUICK_REFERENCE.js - Code snippets
• INTEGRATION_EXAMPLES.js - 5 real-world examples
• INSTALLATION_VISUAL_GUIDE.js - Step-by-step visual

═══════════════════════════════════════════════════════════════════════════════

🎉 YOU'RE DONE!
───────────────────────────────────────────────────────────────────────────────
Your app now has working geolocation! 

Next steps:
1. Test on Android emulator/device ✅
2. Test on iOS simulator/device ✅
3. Integrate into your screens ✅
4. Deploy! 🚀

═══════════════════════════════════════════════════════════════════════════════

📍 SUPPORTED LOCATIONS
───────────────────────────────────────────────────────────────────────────────
Works with:
• GPS coordinates (latitude, longitude)
• Android emulator (Extended Controls)
• iOS simulator (Features > Location)
• Real Android devices with GPS
• Real iPhones with GPS
• Hybrid approach (GPS + Network)

═══════════════════════════════════════════════════════════════════════════════

💡 PRO TIPS
───────────────────────────────────────────────────────────────────────────────
1. Cache location for 5 minutes to reduce API calls
2. Show loading indicator while fetching (can take 5-15 seconds)
3. Handle errors gracefully (permission denied, GPS off)
4. Request location only when user needs it
5. Test on real device for accurate GPS

═══════════════════════════════════════════════════════════════════════════════

🚀 HAPPY CODING!

Questions? Check the documentation files above.
```
